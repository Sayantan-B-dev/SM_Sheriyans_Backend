<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || "Dummy Page" %></title>

  <!-- Basic meta -->
  <meta name="description" content="Dummy EJS template" />

  <!-- Minimal inline style (optional) -->
  <style>
    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      background: #0f172a;
      color: #e5e7eb;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    .card {
      background: #020617;
      padding: 2rem 2.5rem;
      border-radius: 12px;
      max-width: 480px;
      width: 100%;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
    }

    h1 {
      margin: 0 0 0.5rem;
      font-size: 1.5rem;
    }

    p {
      margin: 0.5rem 0;
      line-height: 1.6;
      color: #cbd5f5;
    }

    .footer {
      margin-top: 1.5rem;
      font-size: 0.85rem;
      color: #94a3b8;
    }
  </style>
</head>

<body>
  <div class="card">
    <h1>Hello <%= user?.name || "Guest" %> 👋</h1>

    <p>
      This is a dummy <strong>EJS</strong> template.
    </p>

    <p>
      Server time:
      <strong><%= new Date().toLocaleString() %></strong>
    </p>

    <div class="footer">
      Rendered by Node.js + Express
    </div>
  </div>
</body>
</html>
